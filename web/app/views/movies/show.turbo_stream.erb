<%= turbo_stream.update 'top-app-bar--title' do %>
	<%= truncate(@movie.title, length: 30) %>
<% end %>

<%= turbo_stream.replace 'movie', render(partial: 'movies/movie', locals: { movie: @movie, cinemas: @cinemas }) %>

