(function(){
    let chipEventElem = document.getElementById('<%= dom_id @event, "kinds" %>');

    chipEventElem.innerHTML = '<%= j render partial: 'layouts/classifier/kinds', locals: {event: @event} %>';
})()
